# 功能完善清单

状态只使用：`完成`、`进行中`、`待复核`、`待实现`、`阻塞`。

| 模块 | 状态 | 完成度 | 说明 | 下一步 |
| --- | --- | --- | --- | --- |
| 首页 | 进行中 | 72% | 已做首页标题、白色玻璃感、阴影和拖动手感修复；本轮确认首页可渲染并可打开图库区域。 | 做桌面/移动视觉复核，记录截图差异。 |
| 画布 | 进行中 | 68% | 基础画布、工具栏和项目页可打开；未选本地保存文件夹的预期提示已降级为 warning。 | 验证新增节点、上传、生成、保存、恢复。 |
| 模板 | 进行中 | 78% | API 层模板 mock 生成已写入 generations，前端模板页仍需按钮级验收。 | 复核模板页生成按钮、任务队列、结果区。 |
| 图库 | 进行中 | 75% | 首页图库入口已复核，并复用 `/api/user/generations` 同步后端历史，生成后显示 `共 1 张`。 | 验证多图、删除、保存链接和空状态恢复。 |
| 用户中心 | 进行中 | 68% | 真实表单登录后用户中心显示用户资料、余额、兑换码和 API 线路。 | 验证算力明细、生成记录和头像设置。 |
| 后台 | 进行中 | 74% | Dashboard、users、orders、usage logs、redeem codes、API 线路、模型价格、任务监控、模板工作流、设置已纳入 smoke；真实后台 dashboard 已渲染。 | 继续后台子页面视觉和写操作验收。 |
| 部署 | 进行中 | 60% | 已新增 Docker 轻量部署骨架和数据卷规则。 | 验证容器启动、healthcheck、重启持久化。 |
| New-API | 骨架完成 | 70% | Provider Adapter 默认 New-API，未配置 key 时 mock 回落。 | 使用真实 New-API token 做联通测试。 |
| CPA | 待实现 | 20% | 只作为 New-API 后置渠道写入架构边界，本项目不直接管理。 | 等 New-API 部署后配置渠道。 |
| 文档审查 | 进行中 | 78% | 已建立进度报告、功能清单、review log，并加入复用现有接口/开源工具优先原则。 | 每轮推进持续追加。 |
